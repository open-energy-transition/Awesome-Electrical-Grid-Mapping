name: Deploy Grid Data Explorer to Pages

# Publishes the static site in /docs to GitHub Pages.
# Requires: repo Settings → Pages → Source = "GitHub Actions".

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "README.md"
      - "data/grid-length.csv"
      - "scripts/build_data.mjs"
      - "scripts/build_grid_length.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install build deps
        run: npm ci
      - name: Parse README into the GeoJSON the site fetches at runtime
        run: node scripts/build_data.mjs
      - name: Build the grid-length JSON the site fetches at runtime
        run: node scripts/build_grid_length.mjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs
      - id: deployment
        uses: actions/deploy-pages@v4
