{
  "name": "awesome-electrical-grid-mapping",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:data && npm run build:length",
    "build:data": "node scripts/build_data.mjs",
    "build:admin1": "node scripts/build_admin1.mjs",
    "fetch:length": "node scripts/fetch_grid_length.mjs",
    "build:length": "node scripts/build_grid_length.mjs"
  },
  "devDependencies": {
    "topojson-client": "^3.1.0"
  }
}
